var Filter = require('broccoli-filter');
// var buildHash = require('modernizr/lib/build-hash');
var fs = require('fs');
var path = require('path');

function createCssMap() {
  // buildHash();
};

module.exports = createCssMap;
